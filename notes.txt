1) Import Data into app
2) Find Good, Garbage, Duplicates
3) For Garbage assign Purge (provide a file of all salesforce id)
4) For Duplicates 1st determine type
     - Identical (same first name & last name)
        -if icontact_id exists keep one with icontact_id
        -if "keeper field is empty but to be purged field is not, fill it with details"
        -keeper salesforce_id to merge_id_reference_field

     - Family (same last name)
        -pick 1st full field with icontact_id 
5) 

Sit down and think about the 10 steps 